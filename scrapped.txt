/* topbar */
#topbar {
    position: fixed;
    top: 0; left: 0; right: 0;
    height: 80px;
    overflow: hidden;
    z-index: 1000;
  }
/* Background blur layer with fade */
#topbar .blur-bg {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0); /* transparent */
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    -webkit-mask-image: linear-gradient(to bottom, black 0%, transparent 10%);
    mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
}
/* Content layer on top, sharp */
#topbar .content {
    position: relative;
    z-index: 1;
}
#menu-button {
    font-size: 1.5rem;
    cursor: pointer;
    background: none;
    border: none;
}

#menu{
    width: 300px;
    min-width: fit-content;
    border-width: medium;
    border-style: double;
    border-color: #ff3399;
    margin: 10px;
    padding: 10px;
    position: fixed;
    top: 30px;           /* 10px from the top */
    left: 75%;           /* 25% from the left edge of viewport */
    transform: translateX(-93%); /* shift left by half button width to center on that point */
    z-index: 1000;
}
#menu a:hover{
    font-size: 100%;
}
#menu-button {
    border-style:double;
    border-color:#ff3399;
    position: fixed;
    top: 10px;           /* 10px from the top */
    left: 75%;           /* 25% from the left edge of viewport */
    transform: translateX(-50%); /* shift left by half button width to center on that point */
    z-index: 1000;
}